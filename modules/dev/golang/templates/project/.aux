# TODO: set proper values below, then uncomment

# git config user.name "<full name>"
# git config user.email "<email>"
# git config user.signingkey "<gpg signing key ID>"

# git config url."<url base to use instead>".insteadOf <original url base>
